{
  "name": "studybridge",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"PORT=5001 npm run dev --prefix backend\" \"VITE_API_BASE_URL=http://localhost:5001/api npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
